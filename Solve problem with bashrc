Every time I open a new terminal I get an error if I don't call the next instruction:
$ source /opt/ros/melodic/setup.bash

I have solved doing:

$ nano .bashrc

//add at the end of the file
source /opt/ros/melodic/setup.bash
